{
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": false
  },
  "installCommand": "npm install -g corepack@0.35.0 && corepack enable && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm build",
  "outputDirectory": ".next"
}
