{
  "framework": "nextjs",
  "git": { "deploymentEnabled": false },
  "installCommand": "npm install -g corepack@0.35.0 && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next"
}
