{
  "id": "axel-webhooks-env",
  "name": "Axel — production",
  "values": [
    {
      "key": "baseUrl",
      "value": "https://ingest.axelapp.ai",
      "type": "default",
      "enabled": true
    },
    {
      "key": "sourceId",
      "value": "src_demo",
      "type": "default",
      "enabled": true,
      "description": "The source slug from your dashboard URL (e.g. src_abc123). `src_demo` is a hard-coded test source; create your own at https://app.axelapp.ai/sources"
    },
    {
      "key": "sourceToken",
      "value": "PASTE_YOUR_TOKEN_HERE",
      "type": "secret",
      "enabled": true,
      "description": "The plaintext token shown ONCE on source creation. Format: axt_… For the demo source, ask in chat or check your .env (DEMO_INGEST_TOKEN)."
    },
    {
      "key": "adminToken",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "Operator-only — only needed for the Admin folder. Same value as the worker's ADMIN_TOKEN secret."
    },
    {
      "key": "lastEventId",
      "value": "",
      "type": "default",
      "enabled": true,
      "description": "Set automatically by tests after a successful POST. Useful for replay scenarios."
    }
  ],
  "_postman_variable_scope": "environment"
}
